export const metadata = {
  title: 'Election Simulator',
  description: 'Interactive Nepal election simulator. Adjust vote shares, model demographics, and predict seat allocations for FPTP and PR systems.',
  openGraph: {
    title: 'Election Simulator | NepaliSoch',
    description: 'Interactive Nepal election simulator. Adjust vote shares, model demographics, and predict seat allocations.',
    type: 'website',
  },
};

export default function SimulatorLayout({ children }) {
  return children;
}
