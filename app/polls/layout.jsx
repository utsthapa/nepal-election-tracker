export const metadata = {
  title: 'Polling Data',
  description: 'Aggregated polling data and trends for Nepal elections.',
  openGraph: {
    title: 'Polling Data | NepaliSoch',
    description: 'Aggregated polling data and trends for Nepal elections.',
    type: 'website',
  },
};

export default function PollsLayout({ children }) {
  return children;
}
